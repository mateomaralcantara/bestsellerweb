"use client";
import { FormEvent, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
export function AffiliateForm() { const supabase = useMemo(() => createClient(), []); const [status, setStatus] = useState<string | null>(null); const [loading, setLoading] = useState(false); async function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const formData = new FormData(event.currentTarget); if (!supabase) { setStatus("Conecta Supabase para recibir postulaciones reales."); return; } setLoading(true); const { error } = await supabase.from("affiliate_applications").insert({ full_name: formData.get("full_name"), email: formData.get("email"), channels: formData.get("channels"), audience: formData.get("audience") }); setLoading(false); if (error) { setStatus(error.message); return; } (event.currentTarget as HTMLFormElement).reset(); setStatus("Aplicación enviada. Si conviertes, aquí te van a querer mucho."); } return <form onSubmit={handleSubmit} className="editorial-panel space-y-5 rounded-[32px] p-6"><div className="grid gap-5 md:grid-cols-2"><label className="space-y-2 text-sm text-slate-800"><span>Nombre completo</span><input name="full_name" required className="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition focus:border-brand-400/60" /></label><label className="space-y-2 text-sm text-slate-800"><span>Email</span><input type="email" name="email" required className="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition focus:border-brand-400/60" /></label></div><label className="block space-y-2 text-sm text-slate-800"><span>Canales donde vas a vender</span><input name="channels" placeholder="Instagram, TikTok, newsletter, comunidad..." className="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition focus:border-brand-400/60" /></label><label className="block space-y-2 text-sm text-slate-800"><span>Describe tu audiencia</span><textarea name="audience" rows={5} className="w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-slate-950 outline-none transition focus:border-brand-400/60" /></label><button disabled={loading} className="rounded-full bg-accent-600 px-5 py-3 font-semibold text-white transition hover:scale-[1.01] hover:bg-accent-700 disabled:opacity-60">{loading ? "Enviando..." : "Aplicar como afiliado"}</button>{status ? <p className="text-sm text-accent-700">{status}</p> : null}</form>; }
